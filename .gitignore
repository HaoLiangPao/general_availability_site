# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment variables — NEVER commit secrets
.env
.env.local
.env.*.local

# Database (SQLite — keep locally, back up separately)
bookings.db
bookings.db-shm
bookings.db-wal

# Next.js cache & type generation
next-env.d.ts
.next/types/

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.idea/
.vscode/

# Google OAuth token (if you ever store it locally)
google-token.json
